$(document).ready(function () {

    /**
     $(document).on('click', '.load-branches', function (e) {
        $('a', '#selected-project').removeClass('active');
        $(this).addClass('active');

        e.preventDefault();
    });

     $(document).on('click', '.load-environments', function (e) {
        $('a', '#selected-branch').removeClass('active');
        $(this).addClass('active');

        e.preventDefault();
    });
     */

});